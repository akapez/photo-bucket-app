import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles((theme) => ({
  root: {
    minWidth: 300,
    maxWidth: 345,
    margin: "1rem",       
    
  },
  media: {
    height: 140,
  },
}))
